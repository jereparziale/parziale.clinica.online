import { ClicResaltarFondoDirective } from './clic-resaltar-fondo.directive';

describe('ClicResaltarFondoDirective', () => {
  it('should create an instance', () => {
    const directive = new ClicResaltarFondoDirective();
    expect(directive).toBeTruthy();
  });
});
